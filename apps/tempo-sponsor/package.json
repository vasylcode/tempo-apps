{
	"name": "tempo-sponsor",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test:integration": "node test-sponsor.js",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"check:types": "tsgo --project tsconfig.json --noEmit"
	},
	"dependencies": {
		"ox": "^0.9.16",
		"tempo.ts": "~0.7.3",
		"viem": "^2.40.2"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20251125.0",
		"@types/node": "^24.10.1",
		"typescript": "^5.9.3",
		"wrangler": "^4.50.0"
	}
}
